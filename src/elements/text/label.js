import styled from 'styled-components';

const LabelText = styled.div
`
  margin: 0 10px;
  height: 100%;
  line-height: 25px;
  font-size: 14px;
  font-weight: normal;
`

export default LabelText;